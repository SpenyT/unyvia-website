import '../../styles/componentStyles/hero.css';

export default function Hero() {
    return (
        <section id="hero">
            <div className="hero">
            </div>
        </section>
    );
}